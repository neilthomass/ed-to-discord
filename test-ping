#!/usr/bin/env bash
set -euo pipefail

cd "$(dirname "$0")"

if ! command -v node >/dev/null 2>&1; then
  echo "Missing node. Install Node.js 20 or newer, then try again." >&2
  exit 1
fi

dry_run=false
if [ "${1:-}" = "--dry-run" ]; then
  dry_run=true
  shift
fi

message="${*:-Cloudflare Worker webhook configuration is working.}"

TEST_PING_DRY_RUN="$dry_run" TEST_PING_MESSAGE="$message" node <<'NODE'
const fs = require("fs");

function readEnvFile(path) {
  if (!fs.existsSync(path)) return {};
  const values = {};
  for (const rawLine of fs.readFileSync(path, "utf8").split(/\r?\n/)) {
    const line = rawLine.trim();
    if (!line || line.startsWith("#")) continue;
    const separator = line.indexOf("=");
    if (separator < 1) continue;
    const key = line.slice(0, separator).trim();
    let value = line.slice(separator + 1).trim();
    if ((value.startsWith('"') && value.endsWith('"')) || (value.startsWith("'") && value.endsWith("'"))) {
      value = value.slice(1, -1);
    }
    values[key] = value;
  }
  return values;
}

const values = { ...readEnvFile(".dev.vars"), ...readEnvFile(".env") };
let webhook = values.DISCORD_WEBHOOK_URL;

if (!webhook && values.DISCORD_WEBHOOKS) {
  try {
    webhook = Object.values(JSON.parse(values.DISCORD_WEBHOOKS))[0];
  } catch {
    console.error("DISCORD_WEBHOOKS is not valid JSON.");
    process.exit(1);
  }
}

if (!webhook) {
  console.error("No webhook found. Add DISCORD_WEBHOOK_URL to .env or DISCORD_WEBHOOKS to .dev.vars.");
  process.exit(1);
}

let parsed;
try {
  parsed = new URL(webhook);
} catch {
  console.error("The configured Discord webhook is not a valid URL.");
  process.exit(1);
}

if (parsed.protocol !== "https:" || !["discord.com", "discordapp.com"].includes(parsed.hostname)) {
  console.error("The configured webhook must be an HTTPS Discord webhook URL.");
  process.exit(1);
}

if (process.env.TEST_PING_DRY_RUN === "true") {
  console.log("Webhook configuration is valid. No message was sent.");
  process.exit(0);
}

const payload = {
  username: "Ed",
  allowed_mentions: { parse: [] },
  embeds: [{
    title: "EdToDiscord test",
    description: process.env.TEST_PING_MESSAGE,
    color: 0x66a2ff,
    timestamp: new Date().toISOString(),
  }],
};

fetch(webhook, {
  method: "POST",
  headers: { "Content-Type": "application/json" },
  body: JSON.stringify(payload),
}).then(async (response) => {
  if (!response.ok) {
    throw new Error(`Discord returned ${response.status}: ${await response.text()}`);
  }
  console.log("Test ping sent successfully.");
}).catch((error) => {
  console.error(`Test ping failed: ${error.message}`);
  process.exitCode = 1;
});
NODE
